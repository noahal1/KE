# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Exercise GIF media (© Gym visual, see scripts/download-exercise-gifs.cjs).
# Not redistributed via git — restore with: npm run fetch-gifs
public/gifs/

# Android release signing keystore — NEVER commit. Local backup only.
# GitHub Actions reads it from the ANDROID_KEYSTORE_BASE64 secret instead.
signing/
